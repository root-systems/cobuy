const mapValues = require('lodash/fp/mapValues')
const keyBy = require('lodash/fp/keyBy')

module.exports = {
  create: () => {
    return (options) => {
      const {
        allConsumerIntents,
        supplierCommitment
      } = options

      return 
    }
  }
}
